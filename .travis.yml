# travis ymal file for python3 development

language: python

python:
  - 2.7
  
install:
  - pip install -r requirements.txt
  - python setup.py install
script:
  - ~/poretools/test_data/poretools-test.sh
